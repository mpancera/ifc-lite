---
"@ifc-lite/create": minor
---

Export `emitPolygonProfile`, so a surface that reshapes an existing element
writes the same swept-area profile the builders emit rather than a second
implementation of it.
