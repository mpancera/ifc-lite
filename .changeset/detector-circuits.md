---
"@ifc-lite/create": minor
---

Anchored builders for `IfcDistributionCircuit` and `IfcRelAggregates`, so an
installation can be partitioned into named circuits under its
`IfcDistributionSystem` — a fire-detection system into its Meldergruppen.
